## Weather Widget web component demo.
##### Install yarn. 
```yarn install```
##### Run yarn start, it will open the browser on localmachine with project
```yarn start```
##### Run yarn build, to build project for production that will be available in dist folder.
```yarn build```
